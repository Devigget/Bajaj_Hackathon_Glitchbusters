version: '3.8'
services:
  rag-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./model_cache:/app/model_cache
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped